<div class="container">
  <div class="row">
    <div class="col-md-3 mb-4 text-center bg-light text-dark">
      <h3>管理者ログイン</h3>
    </div>

        <div class="col-md-10 offset-2">
          <%= form_with model: @admin, url: admin_session_path do |f| %>
            <table>
              <tbody>
                <tr>
                  <td class="login"><%= f.label :email, "メールアドレス" %></td>
                  <td class="login"><%= f.email_field :email, autofocus: true, autocomplete: "email", size: "30x1" %></td>
                </tr>
                <tr>
                  <td class="login"><%= f.label :password,"パスワード" %></td>
                  <td class="login"><%= f.password_field :password, autocomplete: "current-password", size: "30x1" %></td>
                </tr>
              </tbody>
            </table>

            <% if devise_mapping.rememberable? %>
              <div class="field">
                <%= f.check_box :remember_me %>
                <%= f.label :remember_me %>
              </div>
            <% end %>

            <div class="col-md-12 text-center">
              <div class="actions">
                <%= f.submit "ログイン", class: "btn btn-primary" %>
              </div>
            </div>
          <% end %>
        </div>

          <%= render "admin/shared/links" %>
      </div>
  </div>
</div>